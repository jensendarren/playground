const assert = require('assert').strict;
const FileService = require('../../service/file');

describe('fileService', () => {
  describe('sanitizes files', () => {
    const fileService = new FileService({});
    it('should work...', () => {
      // TODO
    });
  })
});
